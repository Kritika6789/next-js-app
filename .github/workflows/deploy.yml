name: Deploy to Production

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: Production
    steps:
      - name: Deploy to production
        run: echo "Deploying to production..."
      - name: sending message on slack
        if: success()
        run: |
             curl -X POST -H 'Content-type: application/json' --data '{"text":"Hello, World!"}' https://hooks.slack.com/services/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